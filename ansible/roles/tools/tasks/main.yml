- name: Install node-exporter
  apt:
    name: prometheus-node-exporter
    state: present
  when: inventory_hostname == 'dell'
    
- name: Enable node-exporter
  systemd:
    name: prometheus-node-exporter
    enabled: true
  when: inventory_hostname == 'dell'

- name: Copy Windows reboot script
  copy:
    src: "{{ role_path }}/files/windows-reboot.sh"
    owner: root
    group: root
    mode: '0770'
    dest: /usr/local/bin/windows-reboot
  when: inventory_hostname == 'dell'

- name: Allow kelog to run windows-reboot as root
  blockinfile:
    path: /etc/sudoers
    block: |
      kelog galera = (root) NOPASSWD: /usr/sbin/iwlist,/usr/local/bin/windows-reboot
  when: inventory_hostname == 'dell'

- name: Copy WiFi reset script
  copy:
    src: "{{ role_path }}/files/wifi-reset.sh"
    owner: kelog
    group: kelog
    mode: '0770'
    dest: /usr/local/bin/wifi-reset.sh
  when: inventory_hostname == 'dell'
